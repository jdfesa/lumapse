[package]
name = "lumapse-audit"
version = "0.2.0"
edition = "2021"
description = "Auditor concurrente unificado para Lumapse — código, trazabilidad, schema, docs, jerarquía"

# Dependencias
[dependencies]
regex = "1.10.4"
