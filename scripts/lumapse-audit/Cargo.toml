[package]
name = "lumapse-audit"
version = "0.1.0"
edition = "2021"
description = "Auditor concurrente de código fuente para Lumapse — LOC, TODOs, offline-first"

# Dependencias
[dependencies]
regex = "1.10.4"
